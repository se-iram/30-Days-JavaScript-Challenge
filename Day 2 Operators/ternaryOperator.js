// Ternary Operator Script 
let a = 245
let result;
result = (a >= 0) ? "number is positive" : "number is negative"
console.log(result); 

let b = -456
result = (b >= 0) ? "number is positive" : "number is negative"
console.log(result); 